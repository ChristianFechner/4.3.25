void main() {
  bool ParentalConsent = true;
  int age = 20;
  
  }


 bool movieAgeRating = age >= 18;
 if(movieAgeRating < && ParentalConsent){
  print (` Film schauen erlaubt `);
 } else { (age < 18 );
 print ( `darf den Film nicht schauen `);
 }
